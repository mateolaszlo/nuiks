# Applications

Runnable project applications live here.

- `frontend/` React client
- `gateway/` edge-facing entrypoint
- `file-service/` upload and MinIO integration
- `catalog-service/` PostgreSQL-backed metadata service
- `search-service/` MongoDB-backed search service
- `activity-service/` activity history service

