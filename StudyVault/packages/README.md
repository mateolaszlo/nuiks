# Shared Packages

Reusable code shared across multiple StudyVault apps belongs here.

- `backend-common/` shared FastAPI-facing auth helpers, logging, versioning, structured error helpers, HTTP helpers, and common models including admin-related response types
- `frontend-common/` reserved for future shared frontend code if duplication appears
